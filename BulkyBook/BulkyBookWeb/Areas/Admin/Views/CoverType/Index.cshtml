@model IEnumerable<BulkyBook.Models.CoverType>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">
                Cover Type list
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle-fill"></i> &nbsp; Create New CoverType
            </a>
        </div>
    </div>
</div>

<table class="table table-bordered table-striped w-100">
    <thead>
        <tr>
            <th style="text-align:center">
                Cover Type Name
            </th> 
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td width="40%" style="text-align:center">
                    @obj.Name
                </td> 
                <td width="20%" style="text-align:center">
                    <div class="h-50" role="group">
                        <a asp-controller="CoverType" asp-action="Update" asp-route-id="@obj.ID" style="text-decoration:none">
                            <i class="bi bi-pencil-square"></i>&nbsp; Edit
                        </a>&nbsp;&nbsp;
                        <a asp-controller="CoverType" asp-action="Delete" asp-route-id="@obj.ID" style="text-decoration:none">
                            <i class="bi bi-x-circle"></i>&nbsp; Delete
                        </a>
                    </div>
                </td>
            </tr>

        }
    </tbody>
</table>